import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Padding 먼저 적용")
                .padding()
                .background(Color.yellow)
            
            Text("Background 먼저 적용")
                .background(Color.yellow)
                .padding()
        }
    }
}

#Preview {
    ContentView()
}
